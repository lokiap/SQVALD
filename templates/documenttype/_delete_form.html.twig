<form method="post" action="{{ path('documenttype_delete', {'id': document.id}) }}" style="display: flex"
	  onsubmit="return confirm('Confirmer la suppression de :\n\&quot;{{ document.title }}\&quot; ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document.id) }}">
	<button type="submit" class="btn-del rounded fw-bold align-self-center mb-0 mx-2 h3"><i class="fa fa-trash"></i>
	</button>
</form>
