{% extends 'base.html.twig' %}

{% block title %}DonneesType{% endblock %}

{% block body %}
    <h1>DonneesType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ donnees_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ donnees_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('donnees_type_index') }}">back to list</a>

    <a href="{{ path('donnees_type_edit', {'id': donnees_type.id}) }}">edit</a>

    {{ include('donnees_type/_delete_form.html.twig') }}
{% endblock %}
