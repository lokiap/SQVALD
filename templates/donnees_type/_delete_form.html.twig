<form method="post" action="{{ path('donnees_type_delete', {'id': donnees_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ donnees_type.id) }}">
    <button class="btn">Delete</button>
</form>
